<h2>List of Words</h2>
  <h5>Click on any word to see or add a definition.</h5>
  <h4>placeholder word 2</h4>
  <h4>placeholder word 2</h4>

<% if @list.any? %>
  <% @list.each do |word| %>
  # aiming to call the @list.each returns the three in arrays which is a
  # word.first.first to get word [word[definition]]
  # word.last is the array the array of def.
  # word.last.each do |definition|
    <h4><a href="/words/<%= word.id %>"><%= word.word %><%= word.definition %></a></h4>
  <% end %>
<% end  %>

<h4>Add a new word</h4><br>

<form action="/" method="post">
  <div class="form-group">

    <label for="word">Enter Word</label>
    <input id="word" name="word" class="form-control" type="text"><br>

    <label for="definition">Enter your definition of the word!</label>
    <input id="definition" name="definition" class="form-control" type="text">

  </div>

  <button type="submit" class="btn btn-info">Submit!</button>
</form>
